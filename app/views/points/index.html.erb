<h1><%= t "pointss" %></h1><br>

<table id="datatable" class="table table-striped table-bordered table-hover dataTable no-footer">
  <thead>
    <tr>
      <th><%= t "points.id" %></th>
      <th><%= t "points.company" %></th>
      <th><%= t "points.balance" %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @points.each do |company, balance| %>
      <tr>
        <td><%= company.id %></td>
        <td><%= company.name %></td>
        <td><%= balance %></td>
        <td><%= link_to t(:extract), company_points_path(company.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<script type="text/javascript">
  $(document).ready( function () {
    $('#datatable').DataTable({
      "language": {
        "url": "//cdn.datatables.net/plug-ins/f2c75b7247b/i18n/Portuguese-Brasil.json"
      },
      "bStateSave": true,
      "lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "Todos"]]
    });
  });
</script>